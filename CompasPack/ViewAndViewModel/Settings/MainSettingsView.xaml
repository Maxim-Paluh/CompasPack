<Window x:Class="CompasPack.Settings.MainSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CompasPack.Settings"
        mc:Ignorable="d"
        Title="MainSettingsView" Height="450" Width="800">
    <Window.Resources>
        <!--<DataTemplate DataType="{x:Type local:UserPathSettingsTabViewModel}">
            <local:UserPathSettingsTabView/>
        </DataTemplate>-->

    </Window.Resources>
    <Grid>
        <TabControl
                ItemsSource="{Binding SettingsViewModels}"
                SelectedItem="{Binding SelectedSettingsViewModel,Mode=TwoWay}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Title}"/>
                        <!--<TextBlock Text="*" Visibility="{Binding HasChanges,
              Converter={StaticResource booleanToVisibilityConverter}}"/>-->
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </Grid>
</Window>
